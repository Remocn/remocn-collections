# SCRIPT — sponsor-21st

**VO mode: none (silent narration).** Like its siblings `sponsor-reui` and
`sponsor-reactbits`, this spot has **no spoken voiceover** — the on-screen kinetic
type carries every word, and a `confident minimal tech underscore` BGM plays beneath.
There is no TTS track.

The lines below are the **locked on-screen copy** (the storyboard's `voiceover`
fields), in order. Sentence case, Geist, no letter-spacing / uppercase / eyebrow.
Each is phrase-segmented at its reveal boundaries (`—` / `.` marks a cue).

| # | beat            | on-screen copy (locked)                                        |
|---|-----------------|----------------------------------------------------------------|
| 1 | Hook            | Say hello — to my new sponsor                                   |
| 2 | Reveal / logo   | 21st.dev · The living library of interfaces                    |
| 3 | Gallery stream  | Ten thousand interfaces — heroes, pricing, AI chats, dashboards |
| 4 | Craft pull-back | Every one — hand-crafted by a real design engineer             |
| 5 | Install         | Preview it live. Install with one command.                     |
| 6 | Agent           | For you — and your AI agent                                     |
| 7 | Proof           | 727,000 builders — indie makers to Google, Meta, Vercel        |
| 8 | Outro lockup    | Remocn ✕ 21st.dev · The living library of interfaces           |

**Number rule:** show `727,000` plain — never a `+` suffix (per house style).
Frame 3's "Ten thousand" reads as words; the mark shows `10,000` interfaces.

**BGM:** `confident minimal tech underscore` — restrained, steady forward momentum,
no big drop; a soft swell into the outro lockup. SFX optional and subtle (a low tick
on each gallery card hand-off, a key-click bed under the typed install line).
