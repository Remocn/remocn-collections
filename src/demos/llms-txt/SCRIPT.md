# SCRIPT — llms.txt for AI agents (changelog cut)

Locked narration. The video is text-driven (no VO track on the changelog page), so each line
is also the on-screen copy budget for its beat. Silent beats are omitted.

1. Your agent reads the docs too.
2. Until now it read them like this — by scraping the rendered site.
3. A bad way to learn an API.
4. Now the docs are plain text.
5. At the addresses agents already look for.
6. llms.txt — every docs page, one line and a description.
7. llms-full.txt — the whole documentation, one Markdown file.
8. Or take any docs page — and just add .md.
9. One index. One full corpus. Every page in Markdown.
10. remocn — open source, all the way down.
