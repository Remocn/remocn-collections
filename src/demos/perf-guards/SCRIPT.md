# SCRIPT — Faster site, stronger guards

Locked copy. This is a page video (changelog) with **no spoken VO track** — the video is
text-driven, so each line below is also the on-screen copy budget for its beat. Silent/visual-only
beats are omitted. Numbers stay plain (no "+").

1. Copy-paste UI runs on trust.
2. Faster site, stronger guards.
3. Players now mount only when you scroll to them.
4. The hero video — 18.5 down to 4.7 megabytes.
5. A guard checks the build against the source — every push.
6. Lint, typecheck, tests, the drift guard — on every push.
7. Trusted IPs, capped queues, allowlisted avatars.
8. Faster to load. Safe to render. Always in sync.
9. Remocn — faster and guarded, at remocn.dev.

---

## On-screen labels shown inside the built beats (not narration)

**Frame 5 — the drift guard (source ↔ build compare):**
`registry/blur-out-up` (source) · `blur-out-up.tsx` (built) · a lime check reading "match" · caption "every push"

**Frame 6 — CI checklist (each row gets a settling check):**
Lint · Typecheck · Tests · Drift guard — beneath: "on every push"

**Frame 7 — render-API hardening (three items, beat by beat):**
Trusted IPs · Capped queues · Allowlisted avatars

> These labels are set by the built UI, not spoken — kept out of the numbered copy above so the
> compare panel, the checklist, and the count-down do the talking.
